/** KenGen ICT support contact — MVP uses email only (no in-app ticketing). */
export const SUPPORT_EMAIL = "ict-support@kengen.co.ke";

export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}?subject=${encodeURIComponent(
  "ICT Budgeting Portal — Support Request"
)}`;
